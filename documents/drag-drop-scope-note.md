# Drag-and-Drop Scope Note

The MVP conversion screen uses the file picker only.

Scope decision for task 5.4:

- Keep drag-and-drop disabled for the current MVP.
- Keep drag-in folders out of scope.
- Keep file watching out of scope.
- Revisit drag-and-drop only after the picker-only flow is stable and the interaction model is defined end to end.

This decision matches the current renderer implementation and keeps the user-facing copy aligned with the actual available input flow.
